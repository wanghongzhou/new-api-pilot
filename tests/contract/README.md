# Contract Tests

API and DTO acceptance tests referenced by `docs/acceptance/manifest.yaml` live
in this directory. A planned manifest path must lose its `planned:` prefix only
when the corresponding executable test exists and its evidence workflow is in
place.
